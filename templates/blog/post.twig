{% extends 'base.twig' %}

{% block body %}
    <h1>BLOG POST!</h1>
    <h2>
        {{ post.title }}
    </h2>
    {% if post.is_draft %}This is a draft{% endif %}
    <div>
        {{ post.body }}
    </div>
{% endblock %}