{% extends 'base.twig' %}

{% block body %}
    <img src="{{ this.image }}" id="header-image">

    <div id="content">
        <h1>{{ this.title }}</h1>
        <div>
            {{ this.body }}
        </div>
    </div>
{% endblock %}